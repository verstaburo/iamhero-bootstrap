mixin radio(checked, name)
  +b.radio&attributes(attributes)
    label
      input(type='radio' name=name||'', value='', checked=checked||false)
      block

mixin radio-inline(checked, name)
  +b.LABEL.radio-inline&attributes(attributes)
    input(type='radio' name=name||'', value='', checked=checked||false)
    block
